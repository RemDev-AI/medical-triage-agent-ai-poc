# ==========================================================
# medical-triage-agent-ai-poc/backend/.trivyignore
# ==========================================================

# Exceptions temporaires documentées pour le gate Trivy
# (voir .github/workflows/docker-build-push.yml, step "fail on fixable CRITICAL/HIGH")
#
# Règle : toute entrée ici doit avoir une date de revue et une justification.
# Ne pas ajouter d'entrée sans avoir vérifié qu'aucune version corrigée
# n'est réellement disponible/utilisable dans notre contexte.

# ----------------------------------------------------------
# CVE-2026-39822 - stdlib Go (os.Root symlink following)
# ----------------------------------------------------------
# Composant concerné : binaire vendored `wandb-core` (Go), embarqué dans
# le package Python `wandb`.
# Statut Trivy : "fixed" (1.25.12 / 1.26.5 / 1.27.0-rc.2) mais le correctif
# dépend de la chaine de compilation Go utilisée par l'éditeur `wandb` pour
# builder wandb-core, et n'est pas documenté dans le changelog du package
# Python - impossible de garantir la résolution par un simple bump de version
# `wandb` sans recompilation confirmée en amont.
# Justification du risque accepté : l'exploitation nécessite que notre
# service traite des archives/chemins non fiables via `os.Root` avec suivi
# de symlink. `wandb` est utilisé ici uniquement pour le tracking
# d'expérimentations internes (pas d'ingestion de fichiers utilisateurs
# non fiables via ce composant) - risque jugé faible dans ce contexte.
# Action de suivi : revérifier à chaque bump de version `wandb` si le
# binaire wandb-core embarque un Go >= 1.26.5 (rescanner l'image et retirer
# cette ligne dès que le scan ne remonte plus cette CVE).
# Revue prévue : sous 30 jours, ou au prochain bump de `wandb`.
CVE-2026-39822

# ----------------------------------------------------------
# CVE-2025-47273 - setuptools PackageIndex path traversal
# ----------------------------------------------------------
# Composant concerné : `setuptools`, épinglé à ==77.0.3 dans
# requirements.txt.
# Statut Trivy : "fixed" (78.1.1) mais la contrainte de dépendance de
# `vllm==0.25.1+cpu` impose explicitement `setuptools==77.0.3` ; toute
# version >= 78.1.1 provoque un "ResolutionImpossible" au build pip
# (conflit direct entre les deux contraintes, confirmé en CI/build).
# Justification du risque accepté : la faille exploite
# `PackageIndex.download()` avec une URL contrôlée par un attaquant lors
# d'une installation de paquets. Ce chemin de code n'est exercé que
# pendant `pip install -r requirements.txt` au build de l'image, sur des
# noms de paquets et index que nous contrôlons nous-mêmes (pas
# d'ingestion d'URL utilisateur non fiable via ce composant, ni au build
# ni au runtime de l'API) - risque jugé faible dans ce contexte.
# Action de suivi : revérifier à chaque bump de version `vllm` si la
# contrainte sur `setuptools` a été desserrée en amont (rescanner l'image
# et retirer cette ligne, puis remonter le pin dans requirements.txt, dès
# que le scan ne remonte plus cette CVE).
# Revue prévue : sous 30 jours, ou au prochain bump de `vllm`.
CVE-2025-47273

# ----------------------------------------------------------
# CVE-2026-23949 - jaraco.context path traversal (tar malveillant)
# ----------------------------------------------------------
# Composant concerné : `jaraco.context` 5.3.0, dépendance transitive de
# `setuptools`/`pip` (chaîne de build), pas un paquet applicatif direct.
# Statut Trivy : "fixed" (6.1.0) mais `jaraco.context` est vendored/résolu
# via l'écosystème `setuptools==77.0.3` lui-même épinglé pour `vllm`
# (cf. CVE-2025-47273 ci-dessus) - un bump isolé risque le même conflit
# de résolution pip que `setuptools`.
# Justification du risque accepté : projet POC, pas de traitement
# d'archives tar utilisateur non fiables via ce composant (usage limité
# à la chaîne de build de l'image, sur des sources que nous contrôlons).
# Action de suivi : revérifier au prochain bump de `setuptools`/`vllm`.
# Revue prévue : sous 30 jours, ou au prochain bump de `vllm`.
CVE-2026-23949

# ----------------------------------------------------------
# CVE-2026-24049 - wheel exécution de code via wheel malveillant
# ----------------------------------------------------------
# Composant concerné : `wheel` 0.45.1, dépendance transitive de la chaîne
# de build `setuptools`/`pip` (même contexte que ci-dessus).
# Statut Trivy : "fixed" (0.46.2) mais résolution liée à la même chaîne
# de dépendances épinglée pour `vllm==0.25.1+cpu`.
# Justification du risque accepté : projet POC, aucune installation de
# wheel provenant de sources non fiables/utilisateur - uniquement les
# paquets de notre requirements.txt, sous notre contrôle.
# Action de suivi : revérifier au prochain bump de `setuptools`/`vllm`.
# Revue prévue : sous 30 jours, ou au prochain bump de `vllm`.
CVE-2026-24049
